<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">

    <!-- ── palette ─────────────────────────────────────────── -->
    <Color x:Key="BgColor">#0D0F12</Color>
    <Color x:Key="PanelColor">#14171B</Color>
    <Color x:Key="Panel2Color">#191D22</Color>
    <Color x:Key="Panel3Color">#1F242A</Color>
    <Color x:Key="LineColor">#262C33</Color>
    <Color x:Key="LineSoftColor">#1D2228</Color>
    <Color x:Key="TextColor">#E7EAEE</Color>
    <Color x:Key="MutedColor">#9AA1A9</Color>
    <Color x:Key="FaintColor">#5D646D</Color>
    <Color x:Key="AccentColor">#3FD6C2</Color>
    <Color x:Key="AccentDimColor">#2BA393</Color>
    <Color x:Key="AmberColor">#FFB454</Color>
    <Color x:Key="RedColor">#FF5C5C</Color>
    <Color x:Key="GreenColor">#3FD07A</Color>
    <!-- The one step between Text and Muted. The dialogs had grown eight unnamed greys across that
         gap (#DFE4E9 … #AAB3BC), which is why "secondary text" did not match between two of them. -->
    <Color x:Key="TextDimColor">#CDD3D9</Color>

    <SolidColorBrush x:Key="Bg" Color="{StaticResource BgColor}"/>
    <SolidColorBrush x:Key="Panel" Color="{StaticResource PanelColor}"/>
    <SolidColorBrush x:Key="Panel2" Color="{StaticResource Panel2Color}"/>
    <SolidColorBrush x:Key="Panel3" Color="{StaticResource Panel3Color}"/>
    <SolidColorBrush x:Key="Line" Color="{StaticResource LineColor}"/>
    <SolidColorBrush x:Key="LineSoft" Color="{StaticResource LineSoftColor}"/>
    <SolidColorBrush x:Key="Text" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="Muted" Color="{StaticResource MutedColor}"/>
    <SolidColorBrush x:Key="Faint" Color="{StaticResource FaintColor}"/>
    <SolidColorBrush x:Key="Accent" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentDim" Color="{StaticResource AccentDimColor}"/>
    <SolidColorBrush x:Key="Amber" Color="{StaticResource AmberColor}"/>
    <SolidColorBrush x:Key="Red" Color="{StaticResource RedColor}"/>
    <SolidColorBrush x:Key="Green" Color="{StaticResource GreenColor}"/>
    <SolidColorBrush x:Key="TextDim" Color="{StaticResource TextDimColor}"/>
    <SolidColorBrush x:Key="MeterBg" Color="#0B0D0F"/>

    <!-- The outer edge every dialog window draws. It was #313841 written out in nine files, which
         made it the most-used colour in the app with no name. -->
    <SolidColorBrush x:Key="WindowEdge" Color="#313841"/>

    <!-- Teal-tinted neutrals. The record and restoration surfaces sit on a green-tinted ground and
         had grown a complete second neutral ramp to match it — seven unnamed values doing the work
         of Text/Muted/Faint. It is a deliberate second ramp, so it is named as one rather than
         flattened into the grey it is not. -->
    <SolidColorBrush x:Key="TealBright" Color="#D7F8F2"/>
    <SolidColorBrush x:Key="TealText" Color="#9FD8CF"/>
    <SolidColorBrush x:Key="TealMuted" Color="#7FA69F"/>
    <SolidColorBrush x:Key="TealFaint" Color="#71817C"/>
    <SolidColorBrush x:Key="TealWell" Color="#0F1715"/>
    <SolidColorBrush x:Key="TealWellEdge" Color="#293532"/>
    <SolidColorBrush x:Key="AccentWell" Color="#173A36"/>
    <SolidColorBrush x:Key="AccentEdge" Color="#2E5A54"/>

    <!-- ── type scale ──────────────────────────────────────────
         Six steps. Before this the dialogs used twenty sizes on a half-point continuum, where 10.5,
         11 and 11.5 were all in heavy use and none of them was distinguishable from the others. A
         step is only worth having if two of them are obviously different. -->
    <sys:Double x:Key="LabelSize">9</sys:Double>
    <sys:Double x:Key="SecondarySize">10.5</sys:Double>
    <sys:Double x:Key="BodySize">12.5</sys:Double>
    <sys:Double x:Key="TitleSize">14</sys:Double>
    <sys:Double x:Key="SectionSize">18</sys:Double>
    <sys:Double x:Key="DisplaySize">26</sys:Double>

    <!-- rack effect slots -->
    <SolidColorBrush x:Key="Periwinkle" Color="#8DA4F0"/>
    <SolidColorBrush x:Key="Coral" Color="#E8836B"/>
    <!-- Plugins get a colour of their own, unshared with any built-in category, because the one
         thing worth telling at a glance about a rack slot is whether it is somebody else's code. -->
    <SolidColorBrush x:Key="Violet" Color="#9B8CE0"/>
    <SolidColorBrush x:Key="VioletDim" Color="#45397A"/>
    <SolidColorBrush x:Key="VioletWell" Color="#221D3A"/>
    <SolidColorBrush x:Key="CardEdge" Color="#272E36"/>
    <SolidColorBrush x:Key="WellDeep" Color="#12151A"/>
    <LinearGradientBrush x:Key="CardFaceplate" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#1F242B" Offset="0"/>
        <GradientStop Color="#171B20" Offset="1"/>
    </LinearGradientBrush>

    <!-- Named assembly rather than a bare path. A bare pack path resolves against
         Application.ResourceAssembly, which is whatever host happens to be running, so the fonts and
         the window icon silently fail to load outside the app itself — which is what stopped
         MainWindow being rendered offscreen for verification. -->
    <FontFamily x:Key="UiFont">pack://application:,,,/WaveLab;component/Assets/Fonts/#Inter</FontFamily>

    <FontFamily x:Key="MonoFont">pack://application:,,,/WaveLab;component/Assets/Fonts/#JetBrains Mono</FontFamily>

    <Style x:Key="KeyboardFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Margin="2" BorderBrush="{StaticResource Accent}" BorderThickness="1"
                            CornerRadius="5" SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── tool button ─────────────────────────────────────── -->
    <Style x:Key="ToolButton" TargetType="ButtonBase">
        <Setter Property="Background" Value="{StaticResource Panel3}"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
        <Setter Property="Width" Value="38"/>
        <Setter Property="Height" Value="38"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonBase">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#3A424B"/>
                            <Setter TargetName="bd" Property="Background" Value="#262C33"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#12151A"/>
                        </Trigger>
                        <Trigger Property="ToggleButton.IsChecked" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#2E5A54"/>
                            <Setter TargetName="bd" Property="Background" Value="#173A36"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
                <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Segmented view switch: Waveform / Split / Spectrogram. The checked segment takes the
         accent so the current mode is readable at a glance rather than by elimination. -->
    <!-- ToolButton is a 38×38 icon square; a segment carries a word, so the fixed width has to go or
         "Waveform" renders as "Wa". -->
    <Style x:Key="SegmentButton" TargetType="ToggleButton" BasedOn="{StaticResource ToolButton}">
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Padding" Value="11,0"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="bd" Background="#171B21" BorderBrush="#262D36" BorderThickness="1"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextElement.Foreground="{StaticResource Muted}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#1D232B"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="#33404B"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#193B37"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="#2C6B62"/>
                            <Setter TargetName="bd" Property="TextElement.Foreground" Value="{StaticResource Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RecordArmButton" TargetType="ToggleButton" BasedOn="{StaticResource ToolButton}">
        <Setter Property="Width" Value="56"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Ellipse x:Name="led" Width="6" Height="6" Margin="0,0,7,0" Fill="{StaticResource Faint}"/>
                            <ContentPresenter/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#5E4246"/>
                            <Setter TargetName="bd" Property="Background" Value="#2B2327"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#1B1215"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#7A3030"/>
                            <Setter TargetName="bd" Property="Background" Value="#3D1E1E"/>
                            <Setter TargetName="led" Property="Fill" Value="{StaticResource Red}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButton" TargetType="Button">
        <Setter Property="Foreground" Value="#C8F5EE"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="10" BorderThickness="1" BorderBrush="#2E5A54">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#20504A" Offset="0"/>
                                <GradientStop Color="#173A36" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <!-- Padding is honoured so a button can be sized to its text rather than to a
                             column. The style sets none, so every existing use is unchanged. -->
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#3FD6C2"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
                <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource AccentButton}">
        <Setter Property="Foreground" Value="#FFD9D9"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="10" BorderThickness="1" BorderBrush="#7A3030">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#5C2222" Offset="0"/>
                                <GradientStop Color="#471717" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#FF5C5C"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── window caption buttons ──────────────────────────── -->
    <Style x:Key="CaptionButton" TargetType="Button">
        <Setter Property="Width" Value="46"/>
        <Setter Property="Foreground" Value="{StaticResource Muted}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#22262C"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CaptionCloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#E81123"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── switch toggle ───────────────────────────────────── -->
    <Style x:Key="Switch" TargetType="ToggleButton">
        <!-- Keep the compact 32x17 visual, but give it a forgiving hit target. -->
        <Setter Property="Width" Value="44"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Background="{TemplateBinding Background}">
                        <Border x:Name="track" Width="32" Height="17" CornerRadius="9"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Background="{StaticResource Panel3}" BorderBrush="{StaticResource Line}" BorderThickness="1">
                            <Ellipse x:Name="knob" Width="11" Height="11" Fill="{StaticResource Faint}"
                                     HorizontalAlignment="Left" Margin="2,0,0,0"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="track" Property="Background" Value="#173832"/>
                            <Setter TargetName="track" Property="BorderBrush" Value="#2E5A54"/>
                            <Setter TargetName="knob" Property="Fill" Value="{StaticResource Accent}"/>
                            <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="knob" Property="Margin" Value="0,0,2,0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
                <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── menu ────────────────────────────────────────────── -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#C6CBD1"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="12.5"/>
    </Style>

    <ControlTemplate x:Key="MenuTopTemplate" TargetType="MenuItem">
        <Border x:Name="bd" Background="Transparent" CornerRadius="5" Padding="10,5">
            <Grid>
                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"/>
                <Popup x:Name="popup" Placement="Bottom" IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True" Focusable="False" VerticalOffset="4">
                    <Border Background="{StaticResource Panel2}" BorderBrush="#313841" BorderThickness="1"
                            CornerRadius="9" Padding="5" Margin="8" SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.55" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel3}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel3}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="MenuSubTemplate" TargetType="MenuItem">
        <Grid>
            <Border x:Name="bd" Background="Transparent" CornerRadius="6" Padding="10,6" MinWidth="220">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="check" Text="✓" FontSize="11" Foreground="{StaticResource Accent}"
                               Visibility="Hidden" VerticalAlignment="Center"/>
                    <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True"
                                      VerticalAlignment="Center"/>
                    <TextBlock x:Name="gesture" Grid.Column="2" Text="{TemplateBinding InputGestureText}" Margin="24,0,0,0"
                               Foreground="{StaticResource Faint}" FontFamily="{StaticResource MonoFont}"
                               FontSize="10.5" VerticalAlignment="Center"/>
                    <Path x:Name="submenuArrow" Grid.Column="2" Data="M0,0 L4,4 0,8"
                          Stroke="{StaticResource Faint}" StrokeThickness="1.2" Margin="24,0,2,0"
                          VerticalAlignment="Center" Visibility="Collapsed"/>
                </Grid>
            </Border>
            <Popup x:Name="PART_Popup" Placement="Right" IsOpen="{TemplateBinding IsSubmenuOpen}"
                   AllowsTransparency="True" Focusable="False" HorizontalOffset="-4" VerticalOffset="-8">
                <Border Background="{StaticResource Panel2}" BorderBrush="#313841" BorderThickness="1"
                        CornerRadius="9" Padding="5" Margin="8" SnapsToDevicePixels="True">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.55" Color="#000000"/>
                    </Border.Effect>
                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#242A31"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter TargetName="gesture" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="submenuArrow" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#242A31"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="#C6CBD1"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Template" Value="{StaticResource MenuSubTemplate}"/>
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource MenuTopTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#C6CBD1"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{StaticResource Panel2}" BorderBrush="#313841" BorderThickness="1"
                            CornerRadius="9" Padding="5" Margin="8" SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.55" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Fill="{StaticResource LineSoft}" Margin="8,4"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── combo box ───────────────────────────────────────── -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="#CDD3D9"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="bd" Background="Transparent" CornerRadius="6" Padding="10,7">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#242A31"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="#CDD3D9"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="MaxDropDownHeight" Value="260"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="toggle" Focusable="False" ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bd" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}"
                                            BorderThickness="1" CornerRadius="8">
                                        <Path Data="M0,0 L4,4 8,0" Stroke="{StaticResource Faint}" StrokeThickness="1.4"
                                              HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2,12,0"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="BorderBrush" Value="#3A424B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <!-- ContentTemplateSelector is not decoration: DisplayMemberPath reaches the closed
                             box through the selector rather than through SelectionBoxItemTemplate, so
                             without it every combo bound that way drew the item's ToString(), which for a
                             row class is its type name. The stock template binds all three. -->
                        <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          Margin="12,0,30,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True"
                               Focusable="False" PopupAnimation="Fade" VerticalOffset="4">
                            <Border Background="{StaticResource Panel2}" BorderBrush="#313841" BorderThickness="1"
                                    CornerRadius="9" Padding="5" Margin="8"
                                    MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.55" Color="#000000"/>
                                </Border.Effect>
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── slider ──────────────────────────────────────────── -->
    <Style TargetType="Slider">
        <Setter Property="Height" Value="18"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Border Height="4" CornerRadius="2" Background="{StaticResource Panel3}" VerticalAlignment="Center"/>
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" CornerRadius="2" VerticalAlignment="Center">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="{StaticResource AccentDimColor}" Offset="0"/>
                                                        <GradientStop Color="{StaticResource AccentColor}" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                            </Border>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" Height="18"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Focusable="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Width="10" Height="13" CornerRadius="3" Background="#DFE4E9"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── analysis tabs ───────────────────────────────────── -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource LineSoft}" BorderThickness="0,0,0,1">
                            <TabPanel IsItemsHost="True" Margin="10,0" VerticalAlignment="Center"/>
                        </Border>
                        <ContentPresenter Grid.Row="1" ContentSource="SelectedContent"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource Muted}"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="bd" CornerRadius="6" Padding="14,6" Margin="1,0" Background="Transparent">
                        <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#143F3A"/>
                            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel3}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── file tabs (ListBox) ─────────────────────────────── -->
    <Style x:Key="FileTabs" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Grid Margin="2,0">
                                    <Border x:Name="bd" Background="{StaticResource Panel2}"
                                            BorderBrush="{StaticResource LineSoft}" BorderThickness="1,1,1,0"
                                            CornerRadius="8,8,0,0" Padding="14,0" Height="30" VerticalAlignment="Bottom">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                    <Border x:Name="indicator" Height="2" VerticalAlignment="Top" Margin="1,0"
                                            CornerRadius="2,2,0,0" Background="Transparent"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel3}"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Line}"/>
                                        <Setter TargetName="indicator" Property="Background" Value="{StaticResource Accent}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── checkbox ────────────────────────────────────────── -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="#CDD3D9"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="box" Width="16" Height="16" CornerRadius="4" Background="{StaticResource Panel}"
                                BorderBrush="{StaticResource Line}" BorderThickness="1" VerticalAlignment="Center">
                            <Path x:Name="check" Data="M3,8 L6.5,11.5 13,4.5" Stroke="{StaticResource Accent}" StrokeThickness="1.6"
                                  Visibility="Collapsed" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Margin="0,0,0,1"/>
                        </Border>
                        <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="#173832"/>
                            <Setter TargetName="box" Property="BorderBrush" Value="#2E5A54"/>
                            <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="#3A424B"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── textbox ─────────────────────────────────────────── -->
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="#E7EAEE"/>
        <Setter Property="CaretBrush" Value="{StaticResource Accent}"/>
        <Setter Property="Background" Value="{StaticResource Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="SelectionBrush" Value="#2E5A54"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1" CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="#2E5A54"/>
                        </Trigger>
                        <!-- Without this a disabled field is indistinguishable from a live one: the
                             text stays full brightness and the box keeps its border, so the only way
                             to discover it cannot be typed into is to try. Found by rendering the CD
                             transfer dialog, where the catalogue fields are off for a WAV package. -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── progress bar ────────────────────────────────────── -->
    <Style TargetType="ProgressBar">
        <Setter Property="Height" Value="6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource LineSoft}" BorderThickness="1" CornerRadius="3">
                        <Grid x:Name="PART_Track" ClipToBounds="True">
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="2">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="{StaticResource AccentDimColor}" Offset="0"/>
                                        <GradientStop Color="{StaticResource AccentColor}" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── slim scrollbar ──────────────────────────────────── -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Focusable="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#2E353D" CornerRadius="4" Margin="2,0"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Height" Value="8"/>
                            <Setter Property="Width" Value="Auto"/>
                            <!-- MinHeight as well as Height, or the bar stays 17 px tall however
                                 slim it is asked to be: what this style does not declare falls
                                 through to the built-in ScrollBar style, which sets a minimum from
                                 SystemParameters. Height read 8 and ActualHeight measured 17, and
                                 the difference came out of the file tabs. -->
                            <Setter Property="MinHeight" Value="8"/>
                            <Setter Property="MinWidth" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── settings nav item ───────────────────────────────── -->
    <Style x:Key="NavItem" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource Muted}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="bd" Background="Transparent" CornerRadius="7" Padding="12,8" Margin="0,1">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#143F3A"/>
                            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel3}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
                <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── rack effect power button (LED) ──────────────────── -->
    <Style x:Key="PowerLed" TargetType="ToggleButton">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Background="{TemplateBinding Background}">
                        <Ellipse x:Name="ring" Width="20" Height="20" Fill="{StaticResource Panel}"
                                 Stroke="{StaticResource Line}" StrokeThickness="1"/>
                        <Ellipse x:Name="led" Width="7" Height="7" Fill="{StaticResource Faint}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ring" Property="Stroke" Value="#3A424B"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ring" Property="Fill" Value="#173832"/>
                            <Setter TargetName="ring" Property="Stroke" Value="#2E5A54"/>
                            <Setter TargetName="led" Property="Fill" Value="{StaticResource Accent}"/>
                            <Setter TargetName="led" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.9" Color="{StaticResource AccentColor}"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── rack effect category strip ──────────────────────── -->
    <!-- One cell of the spectral scale switch. A radio button rather than a toggle, because the
         three are exclusive and a keyboard should know it. -->
    <Style x:Key="ScaleSegment" TargetType="RadioButton">
        <Setter Property="FontSize" Value="10.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="face" Background="#181D2C" Padding="11,0" BorderThickness="0,0,1,0"
                            BorderBrush="#2A3557">
                        <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center"
                                   Foreground="{StaticResource Periwinkle}" x:Name="label"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="face" Property="Background" Value="#2A3557"/>
                            <Setter TargetName="label" Property="Foreground" Value="#C7D2FF"/>
                            <Setter TargetName="label" Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="label" Property="Foreground" Value="#C7D2FF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CategoryStrip" TargetType="Border">
        <Setter Property="Width" Value="3"/>
        <Setter Property="CornerRadius" Value="1.5"/>
        <Setter Property="Background" Value="{StaticResource Faint}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Category}" Value="dynamics">
                <Setter Property="Background" Value="{StaticResource Amber}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Category}" Value="eq">
                <Setter Property="Background" Value="{StaticResource Accent}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Category}" Value="time">
                <Setter Property="Background" Value="{StaticResource Periwinkle}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Category}" Value="restoration">
                <Setter Property="Background" Value="{StaticResource Green}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Category}" Value="color">
                <Setter Property="Background" Value="{StaticResource Coral}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Category}" Value="vst3">
                <Setter Property="Background" Value="{StaticResource Violet}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- ── small icon button (rack header controls) ────────── -->

    <Style x:Key="MiniButton" TargetType="ButtonBase">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Faint}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonBase">
                    <Border x:Name="bd" Background="Transparent" CornerRadius="5" Padding="4,2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Panel3}"/>
                            <Setter Property="Foreground" Value="{StaticResource Muted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
                <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── tooltip ─────────────────────────────────────────── -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource Panel2}"/>
        <Setter Property="Foreground" Value="#CDD3D9"/>
        <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{StaticResource Panel2}" BorderBrush="#313841" BorderThickness="1"
                            CornerRadius="7" Padding="10,6">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── shared dialog furniture ─────────────────────────────
         Both of these existed already, several times over: UpperLabel was declared byte-identically
         in two dialogs and written out inline as three separate attributes in fifty-seven more
         places, and the card was three styles under three names (AnalysisCard, SectionCard,
         HardwareCard) differing only by a pixel of radius or padding. One definition each. -->

    <!-- The small bold label that sits above a field. -->
    <Style x:Key="UpperLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Faint}"/>
        <Setter Property="FontSize" Value="{StaticResource LabelSize}"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- A panel that groups related controls inside a dialog. -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LineSoft}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="14"/>
    </Style>

</ResourceDictionary>
